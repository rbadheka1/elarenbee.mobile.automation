# Example Environment Variables

# Appium / Selenium server URL
APPIUM_SERVER_URL=http://localhost:4723/wd/hub

# Device info
ANDROID_DEVICE_NAME=emulator-5554
IOS_DEVICE_NAME=iPhone-12

# Test user credentials
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=Password123

# 3rd party service keys (never commit real secrets)
SERVICE_API_KEY=your-api-key-here

# If required, set ENCRYPTION_KEY for test data decryption (keep this out of version control)
ENCRYPTION_KEY=your-encryption-key-here

---

_Duplicate and rename to `.env`, then set real values locally. Never commit `.env` with secrets or encryption keys. Use a secrets manager for CI/CD._
